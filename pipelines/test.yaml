meta:
  display: My pipeline

node:
  # lists of tags
  requires:
    - build
    - linux
  avoids:
    - mac

tasks:
  # arbitrary name
  - build:
    display: Build the thing
    type: exec
    target: "${workspace}/build.sh"
  # copy build to artifact dir
  - archive:
    type: archive
    target: "${workspace}/build/bin"
